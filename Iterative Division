#include<iostream>
using namespace std;

int div(int x, int y)
{
	int i, count=0;
	int sum=0;
	while(x>=y)
	{
		x=x-y;
		count ++;
	}
	return count;
}

int main()
{
	int a,b;
	cout<<"Enter first number: ";
	cin>>a;
	cout<<"Enter second number: ";
	cin>>b;
	cout<< div(a,b);
	
	return 0;
}
