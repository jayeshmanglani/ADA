#include<iostream>
using namespace std;

int mult(int x, int y)
{
	if (y==1)
	return x;
	return x+mult(x,y-1);
}

int main()
{
	int a,b;
	cout<<"Enter first number: ";
	cin>>a;
	cout<<"Enter second number: ";
	cin>>b;
	cout<< mult (a,b);
	
	return 0;
}
